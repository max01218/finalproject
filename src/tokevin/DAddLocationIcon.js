import { withStyles } from '@material-ui/core/styles';
import AddLocationIcon from '@material-ui/icons/AddLocation';
const DAddLocationIcon = withStyles({
    root: {
        color: 'white'
    },
})(AddLocationIcon)

export default DAddLocationIcon;